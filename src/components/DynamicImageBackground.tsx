import React, {ReactNode} from 'react';
import {StyleProp, ImageBackground, ImageStyle, ImageProps} from 'react-native';

const DynamicImageBackground = ({
  children,
  resizeMode,
  backfaceVisibility,
  borderBottomLeftRadius,
  borderBottomRightRadius,
  backgroundColor,
  borderColor,
  borderRadius,
  borderTopLeftRadius,
  borderTopRightRadius,
  overlayColor,
  tintColor,
  opacity,
  alignContent,
  alignItems,
  alignSelf,
  aspectRatio,
  borderBottomWidth,
  borderEndWidth,
  borderLeftWidth,
  borderRightWidth,
  borderStartWidth,
  borderTopWidth,
  borderWidth,
  bottom,
  display,
  end,
  flex,
  flexBasis,
  flexDirection,
  flexGrow,
  flexShrink,
  flexWrap,
  height,
  justifyContent,
  left,
  margin,
  marginBottom,
  marginEnd,
  marginHorizontal,
  marginLeft,
  marginRight,
  marginStart,
  marginTop,
  marginVertical,
  maxHeight,
  maxWidth,
  minHeight,
  minWidth,
  overflow,
  padding,
  paddingBottom,
  paddingEnd,
  paddingHorizontal,
  paddingLeft,
  paddingRight,
  paddingStart,
  paddingTop,
  paddingVertical,
  position,
  right,
  start,
  top,
  width,
  zIndex,
  direction,
  shadowColor,
  shadowOffset,
  shadowOpacity,
  shadowRadius,
  transform,
  ...rest
}: ImageStyle & ImageProps & {children: ReactNode}) => {
  return (
    <ImageBackground
      style={[
        {
          backfaceVisibility,
          borderBottomLeftRadius,
          borderBottomRightRadius,
          backgroundColor,
          borderColor,
          borderRadius,
          borderTopLeftRadius,
          borderTopRightRadius,
          overlayColor,
          tintColor,
          opacity,
          alignContent,
          alignItems,
          alignSelf,
          aspectRatio,
          borderBottomWidth,
          borderEndWidth,
          borderLeftWidth,
          borderRightWidth,
          borderStartWidth,
          borderTopWidth,
          borderWidth,
          bottom,
          display,
          end,
          flex,
          flexBasis,
          flexDirection,
          flexGrow,
          flexShrink,
          flexWrap,
          height,
          justifyContent,
          left,
          margin,
          marginBottom,
          marginEnd,
          marginHorizontal,
          marginLeft,
          marginRight,
          marginStart,
          marginTop,
          marginVertical,
          maxHeight,
          maxWidth,
          minHeight,
          minWidth,
          overflow,
          padding,
          paddingBottom,
          paddingEnd,
          paddingHorizontal,
          paddingLeft,
          paddingRight,
          paddingStart,
          paddingTop,
          paddingVertical,
          position,
          right,
          start,
          top,
          width,
          zIndex,
          direction,
          shadowColor,
          shadowOffset,
          shadowOpacity,
          shadowRadius,
          transform,
        } as StyleProp<ImageStyle>,
      ]}
      {...rest}>
      {children}
    </ImageBackground>
  );
};

export default DynamicImageBackground;
